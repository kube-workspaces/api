// Code generated by goa, DO NOT EDIT.
//
// kube-workspaces HTTP client CLI support package
//
// Command:
// $ goa gen github.com/kube-workspaces/api/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	healthc "github.com/kube-workspaces/api/gen/http/health/client"
	imagesc "github.com/kube-workspaces/api/gen/http/images/client"
	namespacesc "github.com/kube-workspaces/api/gen/http/namespaces/client"
	sshkeysc "github.com/kube-workspaces/api/gen/http/sshkeys/client"
	volumesc "github.com/kube-workspaces/api/gen/http/volumes/client"
	workspacesc "github.com/kube-workspaces/api/gen/http/workspaces/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"workspaces (list|get|create|delete|start|stop|reset|clone)",
		"volumes (list|get|create|delete)",
		"sshkeys (list|get|create|delete)",
		"images (list|create)",
		"namespaces list",
		"health check",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "workspaces list --namespace \"workspaces\"" + "\n" +
		os.Args[0] + " " + "volumes list --namespace \"workspaces\"" + "\n" +
		os.Args[0] + " " + "sshkeys list --namespace \"chris-at-fordham-id-au\"" + "\n" +
		os.Args[0] + " " + "images list" + "\n" +
		os.Args[0] + " " + "namespaces list" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		workspacesFlags = flag.NewFlagSet("workspaces", flag.ContinueOnError)

		workspacesListFlags         = flag.NewFlagSet("list", flag.ExitOnError)
		workspacesListNamespaceFlag = workspacesListFlags.String("namespace", "workspaces", "")

		workspacesGetFlags         = flag.NewFlagSet("get", flag.ExitOnError)
		workspacesGetNameFlag      = workspacesGetFlags.String("name", "REQUIRED", "Workspace name")
		workspacesGetNamespaceFlag = workspacesGetFlags.String("namespace", "workspaces", "")

		workspacesCreateFlags    = flag.NewFlagSet("create", flag.ExitOnError)
		workspacesCreateBodyFlag = workspacesCreateFlags.String("body", "REQUIRED", "")

		workspacesDeleteFlags         = flag.NewFlagSet("delete", flag.ExitOnError)
		workspacesDeleteNameFlag      = workspacesDeleteFlags.String("name", "REQUIRED", "Workspace name")
		workspacesDeleteNamespaceFlag = workspacesDeleteFlags.String("namespace", "workspaces", "")

		workspacesStartFlags         = flag.NewFlagSet("start", flag.ExitOnError)
		workspacesStartNameFlag      = workspacesStartFlags.String("name", "REQUIRED", "Workspace name")
		workspacesStartNamespaceFlag = workspacesStartFlags.String("namespace", "workspaces", "")

		workspacesStopFlags         = flag.NewFlagSet("stop", flag.ExitOnError)
		workspacesStopNameFlag      = workspacesStopFlags.String("name", "REQUIRED", "Workspace name")
		workspacesStopNamespaceFlag = workspacesStopFlags.String("namespace", "workspaces", "")

		workspacesResetFlags         = flag.NewFlagSet("reset", flag.ExitOnError)
		workspacesResetNameFlag      = workspacesResetFlags.String("name", "REQUIRED", "Workspace name")
		workspacesResetNamespaceFlag = workspacesResetFlags.String("namespace", "workspaces", "")

		workspacesCloneFlags         = flag.NewFlagSet("clone", flag.ExitOnError)
		workspacesCloneBodyFlag      = workspacesCloneFlags.String("body", "REQUIRED", "")
		workspacesCloneNameFlag      = workspacesCloneFlags.String("name", "REQUIRED", "Source workspace name")
		workspacesCloneNamespaceFlag = workspacesCloneFlags.String("namespace", "workspaces", "")

		volumesFlags = flag.NewFlagSet("volumes", flag.ContinueOnError)

		volumesListFlags         = flag.NewFlagSet("list", flag.ExitOnError)
		volumesListNamespaceFlag = volumesListFlags.String("namespace", "workspaces", "")

		volumesGetFlags         = flag.NewFlagSet("get", flag.ExitOnError)
		volumesGetNameFlag      = volumesGetFlags.String("name", "REQUIRED", "Volume name")
		volumesGetNamespaceFlag = volumesGetFlags.String("namespace", "workspaces", "")

		volumesCreateFlags    = flag.NewFlagSet("create", flag.ExitOnError)
		volumesCreateBodyFlag = volumesCreateFlags.String("body", "REQUIRED", "")

		volumesDeleteFlags         = flag.NewFlagSet("delete", flag.ExitOnError)
		volumesDeleteNameFlag      = volumesDeleteFlags.String("name", "REQUIRED", "Volume name")
		volumesDeleteNamespaceFlag = volumesDeleteFlags.String("namespace", "workspaces", "")

		sshkeysFlags = flag.NewFlagSet("sshkeys", flag.ContinueOnError)

		sshkeysListFlags         = flag.NewFlagSet("list", flag.ExitOnError)
		sshkeysListNamespaceFlag = sshkeysListFlags.String("namespace", "", "")

		sshkeysGetFlags         = flag.NewFlagSet("get", flag.ExitOnError)
		sshkeysGetNameFlag      = sshkeysGetFlags.String("name", "REQUIRED", "SSH key name")
		sshkeysGetNamespaceFlag = sshkeysGetFlags.String("namespace", "", "")

		sshkeysCreateFlags    = flag.NewFlagSet("create", flag.ExitOnError)
		sshkeysCreateBodyFlag = sshkeysCreateFlags.String("body", "REQUIRED", "")

		sshkeysDeleteFlags         = flag.NewFlagSet("delete", flag.ExitOnError)
		sshkeysDeleteNameFlag      = sshkeysDeleteFlags.String("name", "REQUIRED", "SSH key name")
		sshkeysDeleteNamespaceFlag = sshkeysDeleteFlags.String("namespace", "", "")

		imagesFlags = flag.NewFlagSet("images", flag.ContinueOnError)

		imagesListFlags = flag.NewFlagSet("list", flag.ExitOnError)

		imagesCreateFlags    = flag.NewFlagSet("create", flag.ExitOnError)
		imagesCreateBodyFlag = imagesCreateFlags.String("body", "REQUIRED", "")

		namespacesFlags = flag.NewFlagSet("namespaces", flag.ContinueOnError)

		namespacesListFlags = flag.NewFlagSet("list", flag.ExitOnError)

		healthFlags = flag.NewFlagSet("health", flag.ContinueOnError)

		healthCheckFlags = flag.NewFlagSet("check", flag.ExitOnError)
	)
	workspacesFlags.Usage = workspacesUsage
	workspacesListFlags.Usage = workspacesListUsage
	workspacesGetFlags.Usage = workspacesGetUsage
	workspacesCreateFlags.Usage = workspacesCreateUsage
	workspacesDeleteFlags.Usage = workspacesDeleteUsage
	workspacesStartFlags.Usage = workspacesStartUsage
	workspacesStopFlags.Usage = workspacesStopUsage
	workspacesResetFlags.Usage = workspacesResetUsage
	workspacesCloneFlags.Usage = workspacesCloneUsage

	volumesFlags.Usage = volumesUsage
	volumesListFlags.Usage = volumesListUsage
	volumesGetFlags.Usage = volumesGetUsage
	volumesCreateFlags.Usage = volumesCreateUsage
	volumesDeleteFlags.Usage = volumesDeleteUsage

	sshkeysFlags.Usage = sshkeysUsage
	sshkeysListFlags.Usage = sshkeysListUsage
	sshkeysGetFlags.Usage = sshkeysGetUsage
	sshkeysCreateFlags.Usage = sshkeysCreateUsage
	sshkeysDeleteFlags.Usage = sshkeysDeleteUsage

	imagesFlags.Usage = imagesUsage
	imagesListFlags.Usage = imagesListUsage
	imagesCreateFlags.Usage = imagesCreateUsage

	namespacesFlags.Usage = namespacesUsage
	namespacesListFlags.Usage = namespacesListUsage

	healthFlags.Usage = healthUsage
	healthCheckFlags.Usage = healthCheckUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "workspaces":
			svcf = workspacesFlags
		case "volumes":
			svcf = volumesFlags
		case "sshkeys":
			svcf = sshkeysFlags
		case "images":
			svcf = imagesFlags
		case "namespaces":
			svcf = namespacesFlags
		case "health":
			svcf = healthFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "workspaces":
			switch epn {
			case "list":
				epf = workspacesListFlags

			case "get":
				epf = workspacesGetFlags

			case "create":
				epf = workspacesCreateFlags

			case "delete":
				epf = workspacesDeleteFlags

			case "start":
				epf = workspacesStartFlags

			case "stop":
				epf = workspacesStopFlags

			case "reset":
				epf = workspacesResetFlags

			case "clone":
				epf = workspacesCloneFlags

			}

		case "volumes":
			switch epn {
			case "list":
				epf = volumesListFlags

			case "get":
				epf = volumesGetFlags

			case "create":
				epf = volumesCreateFlags

			case "delete":
				epf = volumesDeleteFlags

			}

		case "sshkeys":
			switch epn {
			case "list":
				epf = sshkeysListFlags

			case "get":
				epf = sshkeysGetFlags

			case "create":
				epf = sshkeysCreateFlags

			case "delete":
				epf = sshkeysDeleteFlags

			}

		case "images":
			switch epn {
			case "list":
				epf = imagesListFlags

			case "create":
				epf = imagesCreateFlags

			}

		case "namespaces":
			switch epn {
			case "list":
				epf = namespacesListFlags

			}

		case "health":
			switch epn {
			case "check":
				epf = healthCheckFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "workspaces":
			c := workspacesc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
				data, err = workspacesc.BuildListPayload(*workspacesListNamespaceFlag)
			case "get":
				endpoint = c.Get()
				data, err = workspacesc.BuildGetPayload(*workspacesGetNameFlag, *workspacesGetNamespaceFlag)
			case "create":
				endpoint = c.Create()
				data, err = workspacesc.BuildCreatePayload(*workspacesCreateBodyFlag)
			case "delete":
				endpoint = c.Delete()
				data, err = workspacesc.BuildDeletePayload(*workspacesDeleteNameFlag, *workspacesDeleteNamespaceFlag)
			case "start":
				endpoint = c.Start()
				data, err = workspacesc.BuildStartPayload(*workspacesStartNameFlag, *workspacesStartNamespaceFlag)
			case "stop":
				endpoint = c.Stop()
				data, err = workspacesc.BuildStopPayload(*workspacesStopNameFlag, *workspacesStopNamespaceFlag)
			case "reset":
				endpoint = c.Reset()
				data, err = workspacesc.BuildResetPayload(*workspacesResetNameFlag, *workspacesResetNamespaceFlag)
			case "clone":
				endpoint = c.Clone()
				data, err = workspacesc.BuildClonePayload(*workspacesCloneBodyFlag, *workspacesCloneNameFlag, *workspacesCloneNamespaceFlag)
			}
		case "volumes":
			c := volumesc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
				data, err = volumesc.BuildListPayload(*volumesListNamespaceFlag)
			case "get":
				endpoint = c.Get()
				data, err = volumesc.BuildGetPayload(*volumesGetNameFlag, *volumesGetNamespaceFlag)
			case "create":
				endpoint = c.Create()
				data, err = volumesc.BuildCreatePayload(*volumesCreateBodyFlag)
			case "delete":
				endpoint = c.Delete()
				data, err = volumesc.BuildDeletePayload(*volumesDeleteNameFlag, *volumesDeleteNamespaceFlag)
			}
		case "sshkeys":
			c := sshkeysc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
				data, err = sshkeysc.BuildListPayload(*sshkeysListNamespaceFlag)
			case "get":
				endpoint = c.Get()
				data, err = sshkeysc.BuildGetPayload(*sshkeysGetNameFlag, *sshkeysGetNamespaceFlag)
			case "create":
				endpoint = c.Create()
				data, err = sshkeysc.BuildCreatePayload(*sshkeysCreateBodyFlag)
			case "delete":
				endpoint = c.Delete()
				data, err = sshkeysc.BuildDeletePayload(*sshkeysDeleteNameFlag, *sshkeysDeleteNamespaceFlag)
			}
		case "images":
			c := imagesc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
			case "create":
				endpoint = c.Create()
				data, err = imagesc.BuildCreatePayload(*imagesCreateBodyFlag)
			}
		case "namespaces":
			c := namespacesc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
			}
		case "health":
			c := healthc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "check":
				endpoint = c.Check()
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// workspacesUsage displays the usage of the workspaces command and its
// subcommands.
func workspacesUsage() {
	fmt.Fprintln(os.Stderr, `Workspace management service`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] workspaces COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    list: List all workspaces`)
	fmt.Fprintln(os.Stderr, `    get: Get a workspace by name`)
	fmt.Fprintln(os.Stderr, `    create: Create a new workspace`)
	fmt.Fprintln(os.Stderr, `    delete: Delete a workspace`)
	fmt.Fprintln(os.Stderr, `    start: Start a stopped workspace`)
	fmt.Fprintln(os.Stderr, `    stop: Stop a running workspace`)
	fmt.Fprintln(os.Stderr, `    reset: Reset a workspace by re-provisioning it from its image`)
	fmt.Fprintln(os.Stderr, `    clone: Clone an existing workspace under a new name, copying its spec and volume mounts`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s workspaces COMMAND --help\n", os.Args[0])
}
func workspacesListUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] workspaces list", os.Args[0])
	fmt.Fprint(os.Stderr, " -namespace STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List all workspaces`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -namespace STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "workspaces list --namespace \"workspaces\"")
}

func workspacesGetUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] workspaces get", os.Args[0])
	fmt.Fprint(os.Stderr, " -name STRING")
	fmt.Fprint(os.Stderr, " -namespace STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Get a workspace by name`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -name STRING: Workspace name`)
	fmt.Fprintln(os.Stderr, `    -namespace STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "workspaces get --name \"my-workspace\" --namespace \"workspaces\"")
}

func workspacesCreateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] workspaces create", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Create a new workspace`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "workspaces create --body '{\n      \"container\": {\n         \"cpu_limit\": \"2\",\n         \"cpu_request\": \"500m\",\n         \"gpu_request\": \"1\",\n         \"gpu_vendor\": \"nvidia.com/gpu\",\n         \"image\": \"codercom/code-server:latest\",\n         \"memory_limit\": \"2Gi\",\n         \"memory_request\": \"512Mi\",\n         \"name\": \"code-server\",\n         \"port\": 8080\n      },\n      \"env\": [\n         {\n            \"name\": \"PASSWORD\",\n            \"value\": \"changeme\"\n         }\n      ],\n      \"image_pull_policy\": \"IfNotPresent\",\n      \"name\": \"my-workspace\",\n      \"namespace\": \"workspaces\",\n      \"node_selector\": {\n         \"nvidia.com/gpu\": \"true\"\n      },\n      \"shared_memory\": true,\n      \"tolerations\": [\n         {\n            \"effect\": \"NoSchedule\",\n            \"key\": \"nvidia.com/gpu\",\n            \"operator\": \"Equal\",\n            \"value\": \"true\"\n         }\n      ],\n      \"type\": \"container\",\n      \"volume_mounts\": [\n         {\n            \"mount_path\": \"/home/coder\",\n            \"name\": \"my-workspace-data\"\n         }\n      ]\n   }'")
}

func workspacesDeleteUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] workspaces delete", os.Args[0])
	fmt.Fprint(os.Stderr, " -name STRING")
	fmt.Fprint(os.Stderr, " -namespace STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Delete a workspace`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -name STRING: Workspace name`)
	fmt.Fprintln(os.Stderr, `    -namespace STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "workspaces delete --name \"my-workspace\" --namespace \"workspaces\"")
}

func workspacesStartUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] workspaces start", os.Args[0])
	fmt.Fprint(os.Stderr, " -name STRING")
	fmt.Fprint(os.Stderr, " -namespace STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Start a stopped workspace`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -name STRING: Workspace name`)
	fmt.Fprintln(os.Stderr, `    -namespace STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "workspaces start --name \"my-workspace\" --namespace \"workspaces\"")
}

func workspacesStopUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] workspaces stop", os.Args[0])
	fmt.Fprint(os.Stderr, " -name STRING")
	fmt.Fprint(os.Stderr, " -namespace STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Stop a running workspace`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -name STRING: Workspace name`)
	fmt.Fprintln(os.Stderr, `    -namespace STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "workspaces stop --name \"my-workspace\" --namespace \"workspaces\"")
}

func workspacesResetUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] workspaces reset", os.Args[0])
	fmt.Fprint(os.Stderr, " -name STRING")
	fmt.Fprint(os.Stderr, " -namespace STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Reset a workspace by re-provisioning it from its image`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -name STRING: Workspace name`)
	fmt.Fprintln(os.Stderr, `    -namespace STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "workspaces reset --name \"my-workspace\" --namespace \"workspaces\"")
}

func workspacesCloneUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] workspaces clone", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -name STRING")
	fmt.Fprint(os.Stderr, " -namespace STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Clone an existing workspace under a new name, copying its spec and volume mounts`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -name STRING: Source workspace name`)
	fmt.Fprintln(os.Stderr, `    -namespace STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "workspaces clone --body '{\n      \"cpu_limit\": \"1\",\n      \"cpu_request\": \"500m\",\n      \"image\": \"codercom/code-server:4.96.2\",\n      \"memory_limit\": \"2Gi\",\n      \"memory_request\": \"512Mi\",\n      \"new_name\": \"my-workspace-clone\",\n      \"port\": 8080\n   }' --name \"my-workspace\" --namespace \"workspaces\"")
}

// volumesUsage displays the usage of the volumes command and its subcommands.
func volumesUsage() {
	fmt.Fprintln(os.Stderr, `Volume (PVC) management service`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] volumes COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    list: List all volumes`)
	fmt.Fprintln(os.Stderr, `    get: Get a volume by name`)
	fmt.Fprintln(os.Stderr, `    create: Create a new volume (PVC)`)
	fmt.Fprintln(os.Stderr, `    delete: Delete a volume`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s volumes COMMAND --help\n", os.Args[0])
}
func volumesListUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] volumes list", os.Args[0])
	fmt.Fprint(os.Stderr, " -namespace STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List all volumes`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -namespace STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "volumes list --namespace \"workspaces\"")
}

func volumesGetUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] volumes get", os.Args[0])
	fmt.Fprint(os.Stderr, " -name STRING")
	fmt.Fprint(os.Stderr, " -namespace STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Get a volume by name`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -name STRING: Volume name`)
	fmt.Fprintln(os.Stderr, `    -namespace STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "volumes get --name \"my-workspace-data\" --namespace \"workspaces\"")
}

func volumesCreateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] volumes create", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Create a new volume (PVC)`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "volumes create --body '{\n      \"access_mode\": \"ReadWriteOnce\",\n      \"name\": \"my-data\",\n      \"namespace\": \"workspaces\",\n      \"size\": \"10Gi\",\n      \"storage_class\": \"standard\"\n   }'")
}

func volumesDeleteUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] volumes delete", os.Args[0])
	fmt.Fprint(os.Stderr, " -name STRING")
	fmt.Fprint(os.Stderr, " -namespace STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Delete a volume`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -name STRING: Volume name`)
	fmt.Fprintln(os.Stderr, `    -namespace STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "volumes delete --name \"my-workspace-data\" --namespace \"workspaces\"")
}

// sshkeysUsage displays the usage of the sshkeys command and its subcommands.
func sshkeysUsage() {
	fmt.Fprintln(os.Stderr, `SSH public key management service`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] sshkeys COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    list: List the user's SSH public keys`)
	fmt.Fprintln(os.Stderr, `    get: Get an SSH public key by name`)
	fmt.Fprintln(os.Stderr, `    create: Store a new SSH public key`)
	fmt.Fprintln(os.Stderr, `    delete: Delete an SSH public key`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s sshkeys COMMAND --help\n", os.Args[0])
}
func sshkeysListUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] sshkeys list", os.Args[0])
	fmt.Fprint(os.Stderr, " -namespace STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List the user's SSH public keys`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -namespace STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "sshkeys list --namespace \"chris-at-fordham-id-au\"")
}

func sshkeysGetUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] sshkeys get", os.Args[0])
	fmt.Fprint(os.Stderr, " -name STRING")
	fmt.Fprint(os.Stderr, " -namespace STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Get an SSH public key by name`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -name STRING: SSH key name`)
	fmt.Fprintln(os.Stderr, `    -namespace STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "sshkeys get --name \"laptop-2025\" --namespace \"chris-at-fordham-id-au\"")
}

func sshkeysCreateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] sshkeys create", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Store a new SSH public key`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "sshkeys create --body '{\n      \"key_name\": \"Laptop 2025\",\n      \"name\": \"laptop-2025\",\n      \"namespace\": \"chris-at-fordham-id-au\",\n      \"public_key\": \"ssh-ed25519 AAAA... user@host\"\n   }'")
}

func sshkeysDeleteUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] sshkeys delete", os.Args[0])
	fmt.Fprint(os.Stderr, " -name STRING")
	fmt.Fprint(os.Stderr, " -namespace STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Delete an SSH public key`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -name STRING: SSH key name`)
	fmt.Fprintln(os.Stderr, `    -namespace STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "sshkeys delete --name \"laptop-2025\" --namespace \"chris-at-fordham-id-au\"")
}

// imagesUsage displays the usage of the images command and its subcommands.
func imagesUsage() {
	fmt.Fprintln(os.Stderr, `Available workspace images`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] images COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    list: List available workspace images`)
	fmt.Fprintln(os.Stderr, `    create: Create a new image`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s images COMMAND --help\n", os.Args[0])
}
func imagesListUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] images list", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List available workspace images`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "images list")
}

func imagesCreateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] images create", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Create a new image`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "images create --body '{\n      \"category\": \"IDE\",\n      \"default_args\": [\n         \"--bind-addr\",\n         \"0.0.0.0:8080\"\n      ],\n      \"default_cloud_init\": false,\n      \"default_credentials\": {\n         \"password\": \"changeme\",\n         \"username\": \"coder\"\n      },\n      \"default_env\": [\n         {\n            \"name\": \"PASSWORD\",\n            \"value\": \"changeme\"\n         }\n      ],\n      \"default_homedir\": \"/home/coder\",\n      \"default_password\": \"changeme\",\n      \"default_path\": \"/\",\n      \"default_port\": 8080,\n      \"default_shared_memory\": false,\n      \"default_shell\": \"/bin/bash\",\n      \"default_uid\": 1000,\n      \"default_user\": \"coder\",\n      \"default_user_data\": \"\",\n      \"description\": \"Code editor in the browser\",\n      \"homepage_url\": \"https://github.com/coder/code-server\",\n      \"icon\": \"code\",\n      \"image\": \"codercom/code-server:latest\",\n      \"image_homepage_url\": \"https://hub.docker.com/r/codercom/code-server\",\n      \"links\": [\n         {\n            \"title\": \"GitHub\",\n            \"url\": \"https://github.com/coder/code-server\"\n         },\n         {\n            \"title\": \"Docker Hub\",\n            \"url\": \"https://hub.docker.com/r/codercom/code-server\"\n         }\n      ],\n      \"name\": \"Code Server (VS Code)\",\n      \"privileged\": false,\n      \"proxy_config\": {\n         \"custom_request_headers\": {\n            \"x-tenant\": \"workspaces\"\n         },\n         \"inject_base_tag\": true,\n         \"needs_noop_sw\": true,\n         \"preserve_path_prefix\": true,\n         \"rewrite_host_absolute_paths\": false,\n         \"tls_insecure\": false,\n         \"websocket_paths\": [\n            \"/websockify\"\n         ]\n      },\n      \"source_url\": \"https://github.com/coder/code-server\",\n      \"tags\": [\n         \"development\",\n         \"vscode\"\n      ],\n      \"workspace_types\": [\n         \"container\"\n      ]\n   }'")
}

// namespacesUsage displays the usage of the namespaces command and its
// subcommands.
func namespacesUsage() {
	fmt.Fprintln(os.Stderr, `Namespace management`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] namespaces COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    list: List available namespaces`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s namespaces COMMAND --help\n", os.Args[0])
}
func namespacesListUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] namespaces list", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `List available namespaces`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "namespaces list")
}

// healthUsage displays the usage of the health command and its subcommands.
func healthUsage() {
	fmt.Fprintln(os.Stderr, `Health check service`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] health COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    check: Health check endpoint`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s health COMMAND --help\n", os.Args[0])
}
func healthCheckUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] health check", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Health check endpoint`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "health check")
}
